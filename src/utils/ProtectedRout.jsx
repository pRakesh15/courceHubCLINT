// import React from "react";
// import { Navigate, Outlet } from "react-router-dom";

// const ProtectedRout = ({ isAuthenticated }) => {
//   return isAuthenticated ? <Outlet /> : <Navigate to={"/login"} />;
// };

// export default ProtectedRout;
